apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticketing-service.fullname" . }}
  labels:
    app: {{ include "ticketing-service.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "ticketing-service.fullname" . }}
